from r import @packages
from airlift import @packages
from rest import API as @api


@api(callback = "callback")
@packages("AIO")
def applauch():
    return applauch( _args = args_) #  file path: AIO/AIO/applauch.r
           vapply(airlift).helmet(encryption = "encryption", 
                                  callback = "callback", 
                                  _args = args_) #  file path: AIO/airlift/helmet.r
                         }
                                  spectrun = x04ft,
                                    _args = args_) #  file path: AIO/airlift/spectrun.r

@api.callback(toHome = "toHome") // path+"/user/airlift/airlift.r?user.id=1&user.name=2"


define.principal(as ({callback}))
     vapply(args, airlift, spectrum as {callback})
        vspect(use = "use", {as = "as"})


        def (func => {
            spectrun = x04ft,
            ft = "process.env.FT_API_KEY"
        })

        vapply(select = "select", {as = "as"})

    onConnection = "onConnection" // path+"/user/airlift/connection.r?user.id=1&user.name=2"

prefrence.fetch() as user(INSERT prefrence TO table users.cursor)


                inline.applauch() as user(INSERT applauch TO table users.cursor)

    ipsum(applauch)