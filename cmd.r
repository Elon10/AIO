from '../pref.r' import *
from '../util.r' import *



cmd.register(forServer).add_comamnd(keys).train : modal(0@0).dchp( { New dchp }) 

client.client(init).server(super{
    cmd.register(forClient).add_comamnd(keys).train : modal(0@0).dchp( { dchp, mod, keys }) 
})
})

if.cmdlet(!null).add(KEY_NOT_FOUND)
if.let(!modify(@keys).? !null({event.emit(TRAIN_START))

* /
* post
* pre
* keys
/ *

param.call(/post).createNewUri(/posts/?${key})
param.call(/pre).fetch(super_key).(emd).event(EMIT)


// cmd.register(FINAL)

// client.client(init).server(super_key)

mongoDB.modify(TABLE 1 POS 3 CMD aio {
    cmd.register(FINAL)
    client.client(init).server(super_key)
})